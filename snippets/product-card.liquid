<a href="{{ prod.url }}" class="item__card">
    <img src="{{ prod.featured_image | img_url: '1200x', crop:'center'  }}" loading="lazy" width="1200" height="1500" class="w-full mb-4" alt="{{ prod.featured_image.alt }}">
    <div class="item__card-hover glow-bubble"></div>
    <div class="item__card-hover-text">Shop Now</div>
    <div>
        {% if category != blank %}
            <p class="item__card-category block font-italic text-24 -mb-1 lg:text-20">{{ category }} —</p>
        {% endif %}
        <p class="item__card-title block font-matterlight text-24 lg:text-20">{{ prod.title }}</p>
    </div>
    <p class="item__card-price font-matter text-18 text-brown">{{ prod.price | money_without_trailing_zeros }}</p>
</a>